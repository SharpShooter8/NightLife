import { HideOnClickDirective } from './hide-on-click.directive';

describe('HideOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HideOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
