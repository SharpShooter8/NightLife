import { FsqidToPlacePipe } from './fsqid-to-place.pipe';

describe('FsqidToPlacePipe', () => {
  it('create an instance', () => {
    const pipe = new FsqidToPlacePipe();
    expect(pipe).toBeTruthy();
  });
});
