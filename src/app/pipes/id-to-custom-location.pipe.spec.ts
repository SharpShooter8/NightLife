import { IdToCustomLocationPipe } from './id-to-custom-location.pipe';

describe('IdToCustomLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new IdToCustomLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
