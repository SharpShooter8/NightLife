import { UidToProfilePicturePipe } from './uid-to-profile-picture.pipe';

describe('UidToProfilePicturePipe', () => {
  it('create an instance', () => {
    const pipe = new UidToProfilePicturePipe();
    expect(pipe).toBeTruthy();
  });
});
